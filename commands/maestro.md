---
description: Run or resume a Maestro workflow for a feature slug
argument-hint: <slug> [workflow-file] [key=value ...]
---

Load and follow the `maestro` skill (the Maestro lead agent) with these arguments: $ARGUMENTS

The first argument is the feature slug; an optional second argument is the workflow file
(default `workflows/sdlc-main.yaml`); remaining `key=value` pairs are workflow inputs.
